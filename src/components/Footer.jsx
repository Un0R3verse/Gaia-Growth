import React from "react";

export default function Footer() {
  return (
    <footer className="text-center mt-5 mb-3 text-muted">
      <small>© {new Date().getFullYear()} GaiaGrowth — Smart Farming Solutions</small>
    </footer>
  );
}
